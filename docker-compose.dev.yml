version: '3'
services:
  database:
    image: postgres:11.19-alpine
    container_name: loryblu-postgres
    env_file:
      - .env
    volumes:
      - postgres:/var/lib/postgresql/data
    ports:
      - 5432:5432

volumes:
  postgres:
